const handleRestMode        = require('./handeRestMode')
  ,   handleAlignMode       = require('./handleAlignMode')
  ,   handleCalibrationMode = require('./handleCalibrationMode')
  ,   handlePlayMode        = require('./handlePlayMode');

module.exports = {
  handleRestMode,
  handleAlignMode,
  handleCalibrationMode,
  handlePlayMode,
};
